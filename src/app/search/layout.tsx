export const metadata = {
  title: "Story Search | My Fun Stories",
  description: "Search for stories using advanced vector-based semantic search",
};

export default function SearchLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
