import React from "react";


const page = () => {
  return (
    <div>
      hi
      {/* {commentsData.map((comment: any) => (
        <NestedComment key={comment.id} comment={comment} />
      ))} */}
    </div>
  );
};

export default page;
